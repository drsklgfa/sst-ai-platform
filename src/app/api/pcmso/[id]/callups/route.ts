import { NextResponse } from 'next/server'; import { authorizeTenantApi } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { env } from '@/lib/env'; import { generatePcmsoCallups } from '@/lib/pcmso'; import { publicAppUrl } from '@/lib/public-url';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){if(!env.FEATURE_PCMSO)return new Response('Módulo desativado',{status:404});const auth=await authorizeTenantApi('medical.program.manage');if(auth instanceof Response)return auth;const{id}=await params;try{const result=await generatePcmsoCallups({tenantId:auth.tenant.id,workProjectId:id,userId:auth.user.id});await audit({tenantId:auth.tenant.id,userId:auth.user.id,action:'PCMSO_CALLUPS_GENERATED',entityType:'PcmsoProgram',entityId:result.programId,after:result});return NextResponse.redirect(publicAppUrl(`/pcmso/${id}`),303)}catch(error){return new Response(error instanceof Error?error.message:'Falha ao gerar convocações',{status:400})}}
