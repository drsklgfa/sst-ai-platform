import { NextResponse } from 'next/server'; import type { TrainingModality } from '@prisma/client'; import { authorizeTenantApi } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { env } from '@/lib/env'; import { updateTrainingProgram } from '@/lib/training'; import { publicAppUrl } from '@/lib/public-url';
const lines=(v:FormDataEntryValue|null)=>String(v??'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!env.FEATURE_CORPORATE_UNIVERSITY)return new Response('Módulo desativado',{status:404});const auth=await authorizeTenantApi('training.manage');if(auth instanceof Response)return auth;const{id}=await params;const f=await request.formData();try{const row=await updateTrainingProgram({tenantId:auth.tenant.id,workProjectId:id,scope:String(f.get('scope')??''),audience:lines(f.get('audience')),objectives:lines(f.get('objectives')),prerequisites:lines(f.get('prerequisites')),methodology:lines(f.get('methodology')),instructorName:String(f.get('instructorName')??'')||null,technicalResponsibleName:String(f.get('technicalResponsibleName')??'')||null,modality:String(f.get('modality')??'BLENDED') as TrainingModality,workloadMinutes:Number(f.get('workloadMinutes')||0),practicalWorkloadMinutes:Number(f.get('practicalWorkloadMinutes')||0),validityDays:String(f.get('validityDays')??'')?Number(f.get('validityDays')):null,minimumScore:Number(f.get('minimumScore')||70),maxAttempts:Number(f.get('maxAttempts')||3)});await audit({tenantId:auth.tenant.id,userId:auth.user.id,action:'TRAINING_PROGRAM_UPDATED',entityType:'TrainingProgram',entityId:row.id});return NextResponse.redirect(publicAppUrl(`/training/${id}`),303)}catch(error){return new Response(error instanceof Error?error.message:'Falha ao atualizar programa',{status:400})}}
