import { NextResponse } from 'next/server'; import { authorizeTenantApi } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { env } from '@/lib/env'; import { applyTrainingRequirementRules } from '@/lib/training'; import { publicAppUrl } from '@/lib/public-url';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!env.FEATURE_COMPETENCY_MATRIX)return new Response('Módulo desativado',{status:404});const auth=await authorizeTenantApi('training.manage');if(auth instanceof Response)return auth;const{id}=await params;const f=await request.formData();try{const result=await applyTrainingRequirementRules({tenantId:auth.tenant.id,workProjectId:id,dueInDays:Number(f.get('dueInDays')||30),userId:auth.user.id});await audit({tenantId:auth.tenant.id,userId:auth.user.id,action:'TRAINING_RULES_APPLIED',entityType:'TrainingProgram',entityId:id,after:result});return NextResponse.redirect(publicAppUrl(`/training/${id}`),303)}catch(error){return new Response(error instanceof Error?error.message:'Falha ao aplicar regras',{status:400})}}
