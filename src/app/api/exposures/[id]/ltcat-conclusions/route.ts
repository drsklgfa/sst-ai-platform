import type { LtcatExposureConclusion } from '@prisma/client'; import { NextResponse } from 'next/server'; import { authorizeTenantApi } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { env } from '@/lib/env'; import { saveLtcatConclusion } from '@/lib/exposures'; import { publicAppUrl } from '@/lib/public-url';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!env.FEATURE_LTCAT_PPP)return new Response('Módulo desativado',{status:404});const auth=await authorizeTenantApi('technical.conclusion.approve');if(auth instanceof Response)return auth;const{id}=await params;const form=await request.formData();try{const row=await saveLtcatConclusion({tenantId:auth.tenant.id,workProjectId:id,exposurePeriodId:String(form.get('exposurePeriodId')??'')||null,conclusion:String(form.get('conclusion')??'NEEDS_REVIEW') as LtcatExposureConclusion,rationale:String(form.get('rationale')??''),status:String(form.get('status')??'REVIEW') as 'REVIEW'|'APPROVED',professionalId:String(form.get('professionalId')??'')||null});await audit({tenantId:auth.tenant.id,userId:auth.user.id,action:'LTCAT_CONCLUSION_SAVED',entityType:'LtcatTechnicalConclusion',entityId:row.id,after:{status:row.status,conclusion:row.conclusion}});return NextResponse.redirect(publicAppUrl(`/exposures/${id}`),303)}catch(error){return new Response(error instanceof Error?error.message:'Falha ao salvar conclusão',{status:400})}}
