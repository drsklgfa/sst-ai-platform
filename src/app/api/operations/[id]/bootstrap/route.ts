import { NextResponse } from 'next/server'; import { authorizeTenantApi } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { env } from '@/lib/env'; import { getOrCreateOperationalProgram } from '@/lib/operations'; import { publicAppUrl } from '@/lib/public-url';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){if(!env.FEATURE_OPERATIONAL_SST)return new Response('Módulo desativado',{status:404});const auth=await authorizeTenantApi('operations.manage');if(auth instanceof Response)return auth;const{id}=await params;try{const row=await getOrCreateOperationalProgram({tenantId:auth.tenant.id,workProjectId:id,userId:auth.user.id});await audit({tenantId:auth.tenant.id,userId:auth.user.id,action:'OPERATIONAL_PROGRAM_INITIALIZED',entityType:'OperationalSstProgram',entityId:row.id});return NextResponse.redirect(publicAppUrl(`/operations/${id}`),303)}catch(error){return new Response(error instanceof Error?error.message:'Falha ao inicializar operação SST',{status:400})}}
